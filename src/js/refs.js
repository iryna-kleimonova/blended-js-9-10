export const listEl = document.getElementById('task-list');
export const form = document.getElementById('task-form');
export const toggleBtn = document.getElementById('themeToggle');
export const submitBtn = document.querySelector('.header-form-btn');
export const body = document.body;
